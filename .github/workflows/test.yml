name: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04]
        python-version: ["3.10", "3.11", "3.12", "3.13"]
        include:
          # Test on macOS and Windows for Python 3.12 only
          - os: macos-latest
            python-version: "3.12"
          - os: windows-latest
            python-version: "3.12"

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'recursive'
    
    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true
        cache-dependency-glob: "pyproject.toml"
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Set up MATLAB
      if: matrix.os == 'ubuntu-22.04' && matrix.python-version != '3.13'
      uses: matlab-actions/setup-matlab@v2
      with:
        release: R2024b
    
    - name: Add MATLAB to PATH
      if: matrix.os == 'ubuntu-22.04' && matrix.python-version != '3.13'
      shell: bash
      run: |
        echo "${{ runner.tool_cache }}/MATLAB/2024.2.999/x64/bin" >> $GITHUB_PATH
    
    - name: Install Python MATLAB Engine
      if: matrix.os == 'ubuntu-22.04' && matrix.python-version != '3.13'
      shell: bash
      run: |
        cd "${{ runner.tool_cache }}/MATLAB/2024.2.999/x64/extern/engines/python"
        python -m pip install .
    
    - name: Install dependencies
      run: |
        uv pip install --system -e .[all]
    
    - name: Run MATLAB tests
      if: matrix.os == 'ubuntu-22.04' && matrix.python-version != '3.13'
      uses: matlab-actions/run-command@v2
      with:
        command: "ver"
    
    - name: Run Python tests
      env:
        EEGPREP_SKIP_MATLAB: ${{ matrix.python-version == '3.13' && '1' || '0' }}
      run: |
        python -m unittest discover -s tests
    
    - name: Display installed packages
      if: always()
      run: |
        uv pip list --system
