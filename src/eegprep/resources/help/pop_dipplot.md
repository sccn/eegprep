# pop_dipplot

Plot existing DIPFIT dipole model positions.

EEGPrep provides a standalone matplotlib 3-D view for dipoles already stored in `EEG["dipfit"]["model"]`. MRI slice overlays and FieldTrip mesh rendering are not bundled yet, so the plot focuses on component positions, residual variance labels, and coordinate axes.
