# pop_leadfield

Compute a source-model leadfield matrix for DIPFIT/ROI workflows.

EEGLAB delegates leadfield preparation to FieldTrip source-model and head-model routines. EEGPrep exposes the same menu surface and validates required options, then reports that this backend is not bundled in standalone EEGPrep yet.
