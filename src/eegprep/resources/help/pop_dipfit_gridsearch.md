# pop_dipfit_gridsearch

Run a coarse DIPFIT grid search for ICA components.

EEGLAB uses FieldTrip dipole-fitting routines to scan a 3-D grid and reject component fits above a residual-variance threshold. EEGPrep validates channel locations, ICA, and DIPFIT settings, then reports that the FieldTrip backend is not yet bundled.
