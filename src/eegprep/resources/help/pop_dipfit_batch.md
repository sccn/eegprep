# pop_dipfit_batch

Deprecated EEGLAB alias for `pop_dipfit_gridsearch`.

EEGPrep keeps the alias for history/script replay. It delegates to the standalone spherical grid-search backend and returns the same command history as `pop_dipfit_gridsearch`.
