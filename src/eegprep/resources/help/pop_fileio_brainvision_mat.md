# pop_fileio_brainvision_mat

Import a BrainVision Analyzer MATLAB `.mat` file.

EEGLAB exposes this workflow through the optional BrainVision Analyzer I/O
plugin. EEGPrep handles the menu entry through its standalone File-IO/MAT import
path, without depending on an EEGLAB or plugin checkout at runtime.
